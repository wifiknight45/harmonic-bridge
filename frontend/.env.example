# Backend API base (no trailing slash). Leave empty to use same-origin / Vite proxy in dev.
# For GitHub Pages, point this at your hosted FastAPI backend, e.g.:
# VITE_API_URL=https://your-backend.example.com
VITE_API_URL=
